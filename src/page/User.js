import React from 'react';
import Navigation from "../components/Navigation";

const User = () => {
    return (
        <div>
            <Navigation/>
            <h1>User</h1>
        </div>
    );
};

export default User;
